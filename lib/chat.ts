export type Message={id:string;role:"user"|"assistant";content:string};
/** Backend seam: replace with POST /api/chat. Never put API keys in client code. */
export async function chat(messages:Message[]):Promise<string>{
 await new Promise(resolve=>setTimeout(resolve,1100));
 const text=messages.filter(m=>m.role==="user").at(-1)?.content.toLowerCase()??"";
 if(/статус|систем/.test(text))return "Ядро активно, интерфейс работает локально. Анимация и чат готовы. AI-модель пока не подключена: это подготовленный демо-ответ. Сообщения хранятся только в текущей сессии и исчезнут при обновлении страницы.";
 if(/умеешь|возможност/.test(text))return "Сейчас я демонстрирую будущего Хасроя: принимаю сообщения, показываю историю диалога и реагирую движением ядра. После подключения AI-модели здесь можно будет обсуждать идеи, планировать задачи и работать с кодом. Пока мои ответы — локальные заготовки.";
 if(/иде|сайт|проект/.test(text))return "Начнём с трёх вопросов: для кого ваша идея, какую задачу она решает и что должно работать в первой версии? Например, для проекта «Хасрой» первый шаг — живое ядро и удобный чат. Это демонстрационный ответ; свободное обсуждение появится после подключения AI.";
 if(/привет|здравств/.test(text))return "Здравствуйте! Хасрой на связи. Можно проверить статус системы или узнать о возможностях прототипа. Сейчас я отвечаю локальными заготовками — подключение настоящего AI будет следующим этапом.";
 return "Сообщение принято. Сейчас включён демо-режим: я показываю, как будет выглядеть наш диалог, но ещё не анализирую произвольные запросы. Попробуйте спросить «Что ты умеешь?» или «Проверь статус системы».";
}
